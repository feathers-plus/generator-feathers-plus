
// Define TypeScript interface for service `nedb3`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface Nedb3Base {
  // !code: interface // !end
}

// tslint:disable-next-line:no-empty-interface
export interface Nedb3 extends Nedb3Base {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
