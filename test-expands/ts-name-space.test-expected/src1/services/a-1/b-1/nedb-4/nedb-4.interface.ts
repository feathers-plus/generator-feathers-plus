
// Define TypeScript interface for service `nedb4`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface Nedb4Base {
  // !code: interface // !end
}

// tslint:disable-next-line:no-empty-interface
export interface Nedb4 extends Nedb4Base {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
