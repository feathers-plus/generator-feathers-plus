
// Define TypeScript interface for service `users1`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface Users1Base {
  // !<DEFAULT> code: interface
  _id: string;
  name: string;
  // !end
}

// tslint:disable-next-line:no-empty-interface
export interface Users1 extends Users1Base {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
